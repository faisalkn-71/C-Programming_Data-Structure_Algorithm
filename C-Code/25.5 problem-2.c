#include <stdio.h>

int func_sorting(int n, int ara[])
{

    for (int i = 0; i < n; i++)
    {
        // printf("%d ", ara1[i]);
    }
}

int main()
{
    int n, k, i;
    scanf("%d", &n);

    int ara[n];
    for (i = 0; i < n; i++)
    {
        scanf("%d", &ara[i]);
    }
    scanf("%d", &k);

    int sorting = func_sorting(n, ara);

    // int k-largest = func_largest(k,  )
}